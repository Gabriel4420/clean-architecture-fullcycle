{
  "name": "clean-architecture-fullcycle",
  "version": "1.0.0",
  "description": "projeto de estudo sobre clean architecture",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "main": "tsc && node ./dist/main.js",
    "test": "tsc && jest ./dist --watchAll",
    "express": "tsc && node ./dist/src/infra/http/express.js",
    "hapi": "tsc && node ./dist/src/infra/http/hapi.js"
  },
  "devDependencies": {
    "@hapi/hapi": "^20.2.2",
    "@types/jest": "^28.1.4",
    "express": "^4.18.1",
    "jest": "^28.1.2"
  },
  "dependencies": {
    "pg-promise": "^10.11.1"
  }
}
